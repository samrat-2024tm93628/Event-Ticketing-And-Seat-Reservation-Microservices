# Payment Service - Deployment Summary

## 📁 Project Structure

```
payment-service/
├── 📄 README.md              # Quick start guide
├── 📄 DEPLOYMENT.md          # Complete deployment documentation
├── 📄 COMMANDS.sh            # Command reference
├── 🚀 deploy.sh              # Deployment script
├── 🗑️  undeploy.sh            # Undeployment script
├── 🐳 dockerfile             # Docker image definition
├── 📄 .dockerignore          # Docker build optimization
├── 📄 server.js              # Main application
├── 📄 package.json           # Dependencies
├── 📄 generateToken.cjs      # JWT token generator
├── 📄 verifyToken.cjs        # JWT token verifier
├── 📄 .env                   # Environment variables
├── 📁 db/
│   └── init.sql             # Database schema
├── 📁 src/
│   ├── auth/
│   │   └── authMiddleware.js
│   └── db/
│       └── dbClient.js
└── 📁 k8s/
    ├── deployment.yaml      # K8s Deployment
    ├── service.yaml         # K8s Service (NodePort)
    ├── configmap.yaml       # Configuration
    └── secret.yaml          # Secrets
```

## 🚀 Deployment Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    ./deploy.sh                              │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Check Minikube Status                               │
│   ✓ Verify Minikube is running                             │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Configure Docker Environment                        │
│   ✓ Set Docker to use Minikube's daemon                    │
│   $ eval $(minikube docker-env)                             │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 3: Build Docker Image                                  │
│   ✓ Build payment-service:latest                           │
│   $ docker build -t payment-service:latest .                │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 4: Setup PostgreSQL Database                           │
│   ✓ Create payments_db (if not exists)                     │
│   ✓ Initialize schema from db/init.sql                     │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 5: Apply Kubernetes Manifests                          │
│   ✓ ConfigMap (k8s/configmap.yaml)                         │
│   ✓ Secret (k8s/secret.yaml)                               │
│   ✓ Deployment (k8s/deployment.yaml)                       │
│   ✓ Service (k8s/service.yaml)                             │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 6: Wait for Deployment Ready                           │
│   ✓ Wait for pods to be running                            │
│   ✓ Health checks passing                                  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 7: Setup Port Forwarding                               │
│   $ kubectl port-forward service/payment-service 3002:5004  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│              ✅ Service Ready on Port 3002                  │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Undeployment Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   ./undeploy.sh                             │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 1: Stop Port Forwarding                                │
│   ✓ Kill kubectl port-forward processes                    │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 2: Delete Kubernetes Resources                         │
│   ✓ Delete Service                                          │
│   ✓ Delete Deployment                                       │
│   ✓ Delete Secret                                           │
│   ✓ Delete ConfigMap                                        │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 3: Wait for Pods to Terminate                          │
│   ✓ All pods removed                                        │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 4: Optional Docker Image Cleanup                       │
│   ? Prompt to remove Docker image                           │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│              ✅ Service Undeployed                          │
│              (Database preserved)                            │
└─────────────────────────────────────────────────────────────┘
```

## 🌐 Access Methods

### Method 1: Port Forwarding (Recommended)
```bash
kubectl port-forward service/payment-service 3003:3003
# Access: http://localhost:3003
```

### Method 2: NodePort
```bash
# Get Minikube IP
minikube ip  # Example: 192.168.49.2
# Access: http://192.168.49.2:30004
```

## 🧪 Testing Workflow

```
1. Health Check
   ↓
   curl http://localhost:3003/health

2. Generate JWT Token
   ↓
   node generateToken.cjs

3. Test Protected Endpoint
   ↓
   curl -X GET http://localhost:3003/v1/payments/test-protected \
     -H "Authorization: Bearer <TOKEN>"

4. Process Payment
   ↓
   curl -X POST http://localhost:3003/v1/payments/charge \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer <TOKEN>" \
     -d '{
       "order_id": 1,
       "amount": 99.99,
       "method": "credit_card",
       "idempotency_key": "unique-key-123"
     }'

5. Query Payment
   ↓
   curl -X GET http://localhost:3003/v1/payments/1 \
     -H "Authorization: Bearer <TOKEN>"
```

## 🗄️ Database Schema

```sql
payments
├── payment_id (SERIAL PRIMARY KEY)
├── order_id (INTEGER)
├── amount (DECIMAL)
├── method (VARCHAR)
├── status (VARCHAR) [SUCCESS/FAILED/PENDING]
├── reference (VARCHAR)
└── created_at (TIMESTAMP)

idempotency_keys
├── key (VARCHAR PRIMARY KEY)
├── user_id (VARCHAR)
├── response (JSONB)
└── created_at (TIMESTAMP)
```

## 🔑 Environment Configuration

```
┌─────────────────────────┬─────────────────────────────┐
│ Variable                │ Value                       │
├─────────────────────────┼─────────────────────────────┤
│ PORT                    │ 5004                        │
│ DB_HOST                 │ host.minikube.internal      │
│ DB_PORT                 │ 5432                        │
│ DB_USER                 │ postgres (Secret)           │
│ DB_PASS                 │ postgres (Secret)           │
│ DB_NAME                 │ payments_db                 │
│ JWT_SECRET              │ dev_secret_key (Secret)     │
│ JWT_ISSUER              │ https://auth.local/         │
│ JWT_AUDIENCE            │ payment-service             │
└─────────────────────────┴─────────────────────────────┘
```

## 📊 Service Architecture

```
┌──────────────────────────────────────────────────────────┐
│                      Client/User                          │
└───────────────────────┬──────────────────────────────────┘
                        │
                        ↓ HTTP :3003
┌──────────────────────────────────────────────────────────┐
│              Port Forward (kubectl)                       │
└───────────────────────┬──────────────────────────────────┘
                        │
                        ↓ HTTP :3003
┌──────────────────────────────────────────────────────────┐
│         Kubernetes Service (payment-service)              │
│                  NodePort: 30004                          │
└───────────────────────┬──────────────────────────────────┘
                        │
                        ↓
┌──────────────────────────────────────────────────────────┐
│            Kubernetes Pod (payment-service)               │
│  ┌────────────────────────────────────────────────────┐  │
│  │     Node.js Express App (Port 3003)                │  │
│  │     - JWT Authentication                           │  │
│  │     - Payment Processing                           │  │
│  │     - Idempotency                                  │  │
│  └─────────────────────┬──────────────────────────────┘  │
└────────────────────────┼─────────────────────────────────┘
                         │
                         ↓ TCP :5432
┌──────────────────────────────────────────────────────────┐
│         PostgreSQL (Local - via host.minikube.internal) │
│         Database: payments_db                             │
└──────────────────────────────────────────────────────────┘
```

## ⚡ Quick Commands

```bash
# Deploy
./deploy.sh

# Port forward
kubectl port-forward service/payment-service 3003:3003

# Test
curl http://localhost:3003/health

# Generate token
node generateToken.cjs

# View logs
kubectl logs -l app=payment-service -f

# Undeploy
./undeploy.sh
```

## 📚 Documentation Files

- **README.md** - Quick start and overview
- **DEPLOYMENT.md** - Complete deployment guide with all API endpoints and examples
- **COMMANDS.sh** - Reference file with all common commands

## ✅ Success Indicators

After successful deployment, you should see:

```bash
✅ Minikube is running
✅ Docker environment set to Minikube
✅ Docker image built successfully
✅ Database payments_db created/verified
✅ Database schema initialized
✅ All Kubernetes resources applied
✅ Deployment is ready
✅ Service accessible on port 3003
```

Health check should return:
```
Payment Service is running
```

## 🆘 Support

For issues, refer to the Troubleshooting section in DEPLOYMENT.md
